---
source: SEC-022.md
status: structured
createdAt: 2026-04-14T09:17:28.004Z
---
# Fix: cmd_cp/cmd_mv TOCTOU — symlink swap between copy/rename and chown

### Tasks
- [x] [DEV] 在 src/cli/commands.cpp 中新建 safe_chown_path() 辅助函数：对单文件用 open+O_NOFOLLOW+fchown，对目录递归遍历每个条目用 open+O_NOFOLLOW+fchown（O_NOFOLLOW 对目录返回 ELOOP 时 fallback 为普通 chown）
  dependencies: 无
- [x] [DEV] 在 cmd_cp() 中，将 `recursive_chown(chown_target, ai_user)` 替换为 `safe_chown_path(chown_target, ai_user)`
  dependencies: safe_chown_path 辅助函数
- [x] [DEV] 在 cmd_mv() 两个分支（跨文件系统 和 同文件系统）中，将 `recursive_chown(chown_target, ai_user)` 替换为 `safe_chown_path(chown_target, ai_user)`
  dependencies: safe_chown_path 辅助函数
- [x] [QA] code review: 确认 safe_chown_path 对文件拒绝 symlink、对目录正确递归、无 fd 泄露
  dependencies: DEV 全部完成
- [x] [DOC] 更新 README 安全段落，说明 cmd_cp/cmd_mv 使用 O_NOFOLLOW 防 TOCTOU
  dependencies: QA 通过
