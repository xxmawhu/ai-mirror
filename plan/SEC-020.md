---
source: SEC-020.md
status: structured
createdAt: 2026-04-14T09:17:25.364Z
---
# Fix: cmd_cp 使用 cp -a 保留 SUID/SGID 位导致提权

### Tasks
- [x] [DEV] 在 src/cli/commands.cpp cmd_cp() 中，将 `cp -a` 替换为 `cp -r --no-preserve=mode`，保留递归但不保留 SUID/SGID 和文件权限
  dependencies: 无
- [x] [DEV] 在 src/cli/commands.cpp cmd_mv() 跨文件系统分支中，同样将 `cp -a` 替换为 `cp -r --no-preserve=mode`
  dependencies: 无
- [x] [DEV] 在 recursive_chown 之后添加 `chmod -R ug-s` 调用，防御性地清除所有 SUID/SGID 位，作为二次保护
  dependencies: cp -a 替换
- [x] [QA] code review: 确认 cp 命令不再使用 -a flag，确认 chmod ug-s 覆盖 cmd_cp 和 cmd_mv 两个路径
  dependencies: DEV 全部完成
- [x] [DOC] 更新 README 安全段落，说明 cp 操作不再保留 SUID/SGID 位
  dependencies: QA 通过
