---
source: SEC-021.md
status: structured
createdAt: 2026-04-14T09:17:28.737Z
---
# Fix: cmd_touch TOCTOU race — symlink swap between exists check and chown

### Tasks
- [x] [DEV] 在 src/cli/commands.cpp 中新建 safe_chown_file() 辅助函数：使用 open(O_RDONLY|O_NOFOLLOW) 获取 fd，若 fd<0 说明是 symlink 则拒绝；然后用 fchown(fd, uid, gid) 设置所有权；失败时 close(fd) 并返回 false
  dependencies: 无
- [x] [DEV] 在 src/cli/commands.cpp cmd_touch() 中，将 `recursive_chown(file_path, ai_user)` 替换为 `safe_chown_file(file_path, ai_user)`，消除 TOCTOU 窗口
  dependencies: safe_chown_file 辅助函数
- [x] [QA] code review: 确认 O_NOFOLLOW 阻止 symlink 跟随，确认 fd 泄露已处理
  dependencies: DEV 全部完成
- [x] [DOC] 更新 README 安全段落，说明 cmd_touch 使用 fd-based chown 防 TOCTOU
  dependencies: QA 通过
